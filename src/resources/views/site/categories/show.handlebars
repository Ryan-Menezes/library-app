{{>site/_header-default}}

<section class="container list-container">
    {{>site/_sidebar}}

    <div>
        <div class="books">
            {{#each books}}
            {{> site/books/_card book=this.attributes}}
            {{/each}}
        </div>
        
        {{>_pagination}}
    </div>
</section>